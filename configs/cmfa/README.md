# Encrypted configuration artifact

This directory contains an age-encrypted client configuration artifact.

The plaintext source, recipient private key, local paths, and publishing environment are not
part of this repository. The encrypted artifact is generated and validated outside this
repository before publication.
